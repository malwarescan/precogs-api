{
  "@context": "https://schema.org",
  "@type": "Service",
  "name": "{{name}}",
  "description": "{{description}}",
  {{#if provider}}
  "provider": {
    "@type": "{{provider.type}}",
    "name": "{{provider.name}}"
  },
  {{/if}}
  {{#if areaServed}}
  "areaServed": {{areaServed}}
  {{/if}}
}

